{
  "name": "mujmart-root",
  "version": "1.0.0",
  "private": true,
  "description": "MUJMart — Campus Marketplace for MUJ Students",
  "scripts": {
    "dev": "npm-run-all --parallel dev:frontend dev:backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "lint": "npm run lint --prefix frontend",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "setup": "npm run install-all && npm run db:setup",
    "db:setup": "npm run db:generate --prefix backend && npm run db:migrate --prefix backend && npm run db:seed --prefix backend",
    "db:generate": "npm run db:generate --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:reset": "npm run db:reset --prefix backend",
    "db:studio": "npm run db:studio --prefix backend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
